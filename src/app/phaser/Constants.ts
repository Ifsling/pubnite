import GameScene from "./scenes/GameScene"

export const MAP_SCALE_FACTOR = 1.4

export const ammoAmounts: { [key: string]: number } = {
  pistol: 15,
  ak47: 30,
  shotgun: 10,
  sniper: 5,
}

export function SpawnableLocationInGrid(scene: GameScene) {}
